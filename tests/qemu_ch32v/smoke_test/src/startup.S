    .section .init, "ax", @progbits
    .global _start
    .align 2

_start:
    .option push
    .option norelax
    la gp, __global_pointer$
    .option pop

    la sp, _eusrstack

    la a0, _data_lma
    la a1, _data_vma
    la a2, _edata
1:
    bgeu a1, a2, 2f
    lw t0, 0(a0)
    sw t0, 0(a1)
    addi a0, a0, 4
    addi a1, a1, 4
    j 1b

2:
    la a0, _sbss
    la a1, _ebss
3:
    bgeu a0, a1, 4f
    sw zero, 0(a0)
    addi a0, a0, 4
    j 3b

4:
    call main
5:
    j 5b
