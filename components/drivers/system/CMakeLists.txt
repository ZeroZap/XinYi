# System Drivers

# key/rtc currently only contain metadata/stubs; do not add nested CMake targets
# until they own buildable driver sources.

# Legacy duplicate of xy_sys lived here; canonical implementation is
# components/drivers/xy_sys. Keep this directory for system driver stubs only.
