/**
 * @file xy_log.h
 * @brief Logging Stub for HAL Compilation
 */

#ifndef XY_LOG_H
#define XY_LOG_H

/* Minimal logging macros */
#define XY_LOG_DEBUG(fmt, ...)    do { } while(0)
#define XY_LOG_INFO(fmt, ...)     do { } while(0)
#define XY_LOG_WARN(fmt, ...)     do { } while(0)
#define XY_LOG_ERROR(fmt, ...)    do { } while(0)

#endif /* XY_LOG_H */
