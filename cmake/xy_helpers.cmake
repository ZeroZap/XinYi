# XinYi CMake helper functions

# xy_add_srcs(<sources...>)
# Source files are collected via GLOB_RECURSE at the component level,
# so sub-component calls to xy_add_srcs are no-ops.
function(xy_add_srcs)
endfunction()
